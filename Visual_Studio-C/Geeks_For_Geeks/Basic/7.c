/*C Program to Print the ASCII Value of a Character*/

#include <stdio.h>

int main()
{

    char character;
    printf("Enter a character:");
    scanf("%c", &character);

    printf("ASCII Value of a Character: %d", character);
        
    return 0;
}